# Generic libraries only — no MCP SDKs (protocol is implemented manually per project requirements).
httpx>=0.27
python-dotenv>=1.0

# Official Git MCP server (Anthropic) — an external tool we connect to as a
# subprocess, not a library our own code imports. That it's built with the
# official MCP SDK internally doesn't matter: our client-side protocol
# implementation (mcp_client/) still never imports mcp/FastMCP.
mcp-server-git>=2026.8
